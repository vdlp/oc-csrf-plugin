<?php

declare(strict_types=1);

return [

    /*
    |--------------------------------------------------------------------------
    | Exclude Paths
    |--------------------------------------------------------------------------
    |
    | Add paths to exclude CSRF token validation (not recommended).
    |
    */

    'exclude_paths' => [
        // i.e. '/path/to/exclude'
    ],

];
